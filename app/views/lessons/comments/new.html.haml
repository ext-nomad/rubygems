.card.shadow
  .card-header
    = t('title.add_comment')
    = @lesson
  .card-body
    = render 'lessons/comments/form'
  .card-footer
    = link_to t('global.buttons.back'), course_lesson_path(@course, @lesson)
