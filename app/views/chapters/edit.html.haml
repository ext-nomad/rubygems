- content_for :title do
  = t('title.title')
  \/
  = t('title.course')
  = @course.title
  \/
  = t('title.editing_chapter')

%ol.breadcrumb.rounded
  %li.breadcrumb-item= link_to t('title.courses'), courses_path
  %li.breadcrumb-item= link_to @course.title, course_path(@course)
  %li.breadcrumb-item= link_to t('title.editing_chapter'), edit_course_chapter_path(@course, @chapter)

.card.shadow
  .card-header
    %h3
      .far.fa.fa-check-square
      = t('title.editing_chapter')
  .card-body
    = render 'form'
  .card-footer
    = link_to t('global.buttons.back'), course_path(@course)
